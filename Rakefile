require 'rubygems'
require 'rake'
require 'echoe'

Echoe.new('conversio', '0.1.0') do |p|
  p.description    = "Renders Markdown plain text files to HTML"
  p.url            = "notyet"
  p.author         = "Victor Penso"
  p.email          = "v.penso@gsi.de"
  p.ignore_pattern = ["tmp/*", "script/*"]
  p.development_dependencies = []
end
